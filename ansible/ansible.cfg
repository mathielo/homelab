[defaults]
inventory = inventory.ini
vars_plugins_enabled = host_group_vars,community.sops.sops
# Adopt the ansible-core 2.24 default early. Facts are still gathered and
# accessible via ansible_facts['name']; only the top-level ansible_* injection
# is disabled. None of our plays reference gathered facts that way.
inject_facts_as_vars = False
