# Split-DNS for k3s services → MetalLB VIP.
#
# The leading `*.` matches every subdomain at any depth but NOT the bare apex,
# which stays on public DNS for the website.
address=/*.m6o.dev/10.10.50.3

# Public subdomains, handed back to the upstream resolver.
server=/r2.m6o.dev/#
