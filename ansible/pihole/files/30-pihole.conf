# pihole.m6o.dev → the keepalived VIP, so whichever node is active serves its own
# admin UI. More specific than the 20-k3s.conf `address=/m6o.dev/...` wildcard, so
# dnsmasq prefers these (longest-match wins).
address=/pihole.m6o.dev/10.10.53.53
address=/pihole.m6o.dev/2001:2042:37b0:1c35::53
