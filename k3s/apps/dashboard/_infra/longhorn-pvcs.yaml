---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: uptime-kuma-data-lh
  namespace: dashboard
spec:
  storageClassName: longhorn
  accessModes: [ReadWriteOnce]
  resources:
    requests:
      storage: 2Gi
