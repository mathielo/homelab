---
- name: Deploy Cloudflare Tunnel
  hosts: k3s_servers
  become: true

  vars_prompt:
    - name: tunnel_token
      prompt: "Enter your Cloudflare Tunnel token"
      private: true

  tasks:
    - name: Create or update tunnel token secret
      command: >
        k3s kubectl create secret generic cloudflared-token
        --namespace kube-extra
        --from-literal=TUNNEL_TOKEN={{ tunnel_token }}
        --dry-run=client -o yaml
      register: secret_manifest
      changed_when: false
      no_log: true

    - name: Apply tunnel token secret
      command: k3s kubectl apply -f -
      args:
        stdin: "{{ secret_manifest.stdout }}"
      no_log: true

    - name: Copy cloudflared deployment manifest
      copy:
        src: files/cloudflared/deployment.yml
        dest: /tmp/cloudflared-deployment.yml
        mode: "0644"

    - name: Apply cloudflared deployment
      command: k3s kubectl apply -f /tmp/cloudflared-deployment.yml

    - name: Clean up manifest file
      file:
        path: /tmp/cloudflared-deployment.yml
        state: absent

    - name: Wait for cloudflared pod to be ready
      command: >
        k3s kubectl wait --namespace kube-extra
        --for=condition=Available
        deployment/cloudflared
        --timeout=60s
      changed_when: false

    - name: Show tunnel status
      debug:
        msg: >-
          Cloudflare Tunnel deployed! Check the Cloudflare Zero Trust dashboard
          to verify the tunnel shows as "Healthy".
