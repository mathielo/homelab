---
apiVersion: v1
kind: Secret
metadata:
  name: cloudflared-token
  namespace: cloudflared
type: Opaque
data:
  # Base64-encode your tunnel token from the Cloudflare dashboard:
  #   echo -n 'your-token-here' | base64
  TUNNEL_TOKEN: REPLACE_WITH_BASE64_ENCODED_TOKEN
