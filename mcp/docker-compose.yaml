services:
  homelab-mcp:
    image: bjeans/homelab-mcp:3.0.0
    container_name: homelab-mcp
    network_mode: host
    stdin_open: true
    tty: true
    env_file:
      - .env
    restart: unless-stopped
